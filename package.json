{
  "name": "hash-space",
  "version": "1.0.2",
  "description": "Hash Space is a DeFi product discovery game where players explore a galaxy of defi planets, get yield bounties, flying around in a spaceship powered with footsteps.",
  "main": "index.js",
  "engines": {
    "node": "16.x",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "lerna run build --parallel --stream",
    "test": "lerna run test --stream",
    "node": "lerna run node --stream",
    "dev": "lerna run dev --stream",
    "seed:local": "hardhat hello --network localhost",
    "seed:mumbai": "hardhat hello --network matic",
    "deploy:local": "lerna run deploy:local --stream",
    "deploy:mumbai": "lerna run deploy:mumbai --stream",
    "deploy:emerald-test": "lerna run deploy:emerald-test --stream",
    "deploy:arbitrum-nitro": "lerna run deploy:arbitrum-nitro --stream",
    "clean": "npm exec --workspaces -- npx rimraf node_modules && npx rimraf node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hash-space/hash-space.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hash-space/hash-space/issues"
  },
  "homepage": "https://github.com/hash-space/hash-space#readme",
  "workspaces": [
    "*"
  ],
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
